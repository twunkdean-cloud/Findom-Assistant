# Frontend (Vercel) environment variables
# Set these in Vercel → Project → Settings → Environment Variables
VITE_SUPABASE_URL=https://qttmhbtaguiioomcjqbt.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Supabase Edge Function secrets (set in Supabase Console → Edge Functions → Manage Secrets)
# Do NOT put these in Vercel; they belong in Supabase only.
# GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Optional: Restrict CORS for your edge function (set in Supabase secrets)
# ALLOWED_ORIGIN=https://your-project-name.vercel.app